import { NavBar } from "@/widgets";

function HomePage() {
  return (
    <section>
      <header>
        <NavBar />
      </header>
    </section>
  );
}

HomePage.displayName = "/src/pages/HomePage.jsx";

export default HomePage;
